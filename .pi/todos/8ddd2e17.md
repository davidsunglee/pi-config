{
  "id": "8ddd2e17",
  "title": "Workflow polish: worktree UX, escalation, model overrides",
  "tags": [
    "execute-plan",
    "generate-plan",
    "workflow",
    "ux",
    "worktree",
    "escalation",
    "skills",
    "models"
  ],
  "status": "open",
  "created_at": "2026-04-13T20:00:00.000Z",
  "assigned_to_session": "019d9c81-3f26-747a-b016-63bb2e33c6f2"
}

## 1. Execution handoff and follow-through

### Worktree reuse UX and safety — completed

**Completed:** `execute-plan` now logs reused workspace path + reason, checks for dirty reused workspaces, auto-proceeds when clean, and offers `continue / quit / create a new worktree instead` when reuse is dirty. The `(n)` path now drops into the normal new-worktree flow, and the Step 0 / Step 3 wording was aligned and reviewed.

**Notes:** This todo item is considered complete for the scoped workspace reuse UX/safety work that was implemented from `.pi/specs/2026-04-18-workspace-reuse-ux-safety.md` and landed in `feat(execute-plan): add workspace reuse safety checks` (`946be3d`).

### BLOCKED escalation should be immediate — completed

**Completed:** `execute-plan` now surfaces `BLOCKED` outcomes before later waves regardless of wave pacing, drains already-running same-wave tasks first, shows a combined blocked-task escalation view with same-wave outcome context, and withholds Step 10 / Step 11 until all blocked work is resolved or the user stops execution.

**Notes:** This todo item is considered complete for the scoped blocked-task escalation work implemented from `.pi/specs/2026-04-18-blocked-task-escalation.md`, executed via `.pi/plans/done/2026-04-18-blocked-task-escalation.md`, and reviewed in `.pi/reviews/2026-04-18-blocked-task-escalation-code-review.md`.

*Reinforced by the 2026-04-17 workflow evaluation (parent `7e1fa3d2`), which re-surfaced this as a top-priority gap.*

## 2. Model matrix resolution order

### `model-tiers.json` should support project-local override — open

**Problem:** Both `generate-plan` and `execute-plan` read model tiers from `~/.pi/agent/model-tiers.json`. This is a user-level config with no way to override per project. Different projects may need different model assignments (for example, a large codebase might need `capable` where a smaller one uses `standard`).

**Suggestion:** Look for `.pi/agent/model-tiers.json` in the project directory first, then fall back to `~/.pi/agent/model-tiers.json`. This matches the existing pi convention where project-level config (`.pi/`) takes precedence over user-level config (`~/.pi/`).
