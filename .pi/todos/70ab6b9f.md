{
  "id": "70ab6b9f",
  "title": "Update whimsical extension to use phrases from ai-phrases.txt",
  "tags": [
    "extension",
    "whimsical"
  ],
  "status": "done",
  "created_at": "2026-04-06T02:06:51.233Z"
}

## Overview

Replace the entire hardcoded `messages` array in `~/.pi/agent/extensions/whimsical.ts` with the 183 phrases from `/Users/david/Code/pi-config/ai-phrases.txt`.

## Details

- **Replace entire array** — remove both the short and long multi-word phrase sections
- **Deduplicate** — the ai-phrases.txt list is the source of truth; no duplicates should appear
- **Append `...`** — each bare word from the file (e.g. `Baking`) becomes `"Baking..."` in the array
- **One-time paste** — embed the phrases directly in the code; do not read the file at runtime
- The rest of the extension (`pickRandom`, `turn_start`, `turn_end` handlers) stays unchanged
