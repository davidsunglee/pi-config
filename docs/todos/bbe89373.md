{
  "id": "bbe89373",
  "title": "Adapt rpiv-pi research into a non-interactive scout brief for planning",
  "tags": [
    "workflow",
    "scout",
    "research",
    "planner",
    "subagents",
    "token-cost"
  ],
  "status": "open",
  "created_at": "2026-04-14T00:00:00.000Z"
}

## Problem

Every subagent in the workflow starts with fresh context and independently discovers codebase structure. The planner does the deepest exploration — following imports, tracing data flow, understanding interfaces — and this is the most expensive read work in the pipeline. Coders in the same wave often re-read overlapping files (shared interfaces, shared types). `define-spec` also needs enough codebase reality to ask good questions. Nobody shares notes.

`rpiv-pi` already has most of the reconnaissance mechanics this workflow needs in its `research` skill:

Source reference: https://github.com/juicesharp/rpiv-mono/blob/main/packages/rpiv-pi/skills/research/SKILL.md

- `scope-tracer` reads mentioned artifacts, sweeps anchor terms, reads 5–10 key files, and emits a discovery summary plus dense trace-quality questions.
- `research` groups those questions, dispatches analysis agents, synthesizes code references, integration points, architecture insights, precedents/lessons, developer context, and open questions.
- The resulting artifact is a durable, reviewable compression of task-scoped codebase reality.

The opportunity is to **adapt those research mechanics into a scout stage** for this repo's workflow, rather than designing a scout agent from scratch.

## Proposal

Add an optional **scout** stage that runs before mandatory `define-spec`. Given a todo or freeform task, the scout performs task-scoped codebase reconnaissance and writes an optional structured **scout brief** that `define-spec` and downstream planning/review agents consume to avoid repeating broad exploratory reads.

The scout is **task-aware, not task-blind**. It should receive the full todo/freeform input so it can focus reconnaissance on the specific work being considered. It is not a whole-codebase indexer by default; it is scoped to the task at hand: find the files, interfaces, dependency chains, patterns, tests, precedents, and risk areas relevant to this specific piece of work.

To avoid overfitting to the todo's wording, the scout prompt should require:

- a short **broad orientation pass** before deep-diving: repo layout, package/module boundaries, likely entry points, test structure, and nearby areas that might be relevant;
- a task-focused deep dive based on the todo/freeform input; and
- an explicit **disconfirmation step**: check whether named files are incomplete or misleading, search for adjacent implementations and alternate call paths, record contradictions between the task framing and code reality, and call out possible misses.

Implementation direction:

- Reuse/adapt the useful parts of `rpiv-pi`'s `research` flow: scope tracing, anchor sweeps, grouped analysis, code references, integration points, precedents, and open questions.
- Do **not** copy the research skill wholesale. A scout brief needs a tighter output contract, deterministic path, non-interactive behavior, direct integration with `define-spec`, and spec-provenance handoff into planning.
- Prefer a scout-specific agent/prompt over a generic research artifact so downstream consumers can rely on stable sections.
- Encode broad-orientation and disconfirmation requirements directly in the scout prompt so every brief has the same anti-bias behavior.

## Where it fits in the pipeline

```text
todo/freeform → [scout brief] → define-spec Q&A → spec → generate-plan → execute-plan
```

Only the scout brief is optional. `define-spec` is mandatory in the normal workflow because it owns the user Q&A loop for intent, scope, and architecture decisions. The preferred flow for ambiguous or non-trivial work is:

```text
todo/freeform → optional scout brief → define-spec Q&A → spec → generate-plan
```

A future lightweight path may still exist for simple work, but it should start after mandatory user Q&A, for example:

```text
todo/freeform → define-spec Q&A → lightweight plan → lightweight serial execution
```

That lightweight path should still keep quality gates, but it likely should not use the full parallel `generate-plan → execute-plan` machinery.

The brief flows into downstream stages:

- **define-spec** consumes the brief to ground its Q&A in actual codebase reality.
- **planner** consumes the brief to skip broad discovery and focus on plan structure, task decomposition, and task-specific verification.
- **plan-reviewer** consumes the brief to check whether the plan incorporated important constraints, risk areas, tests, and patterns from reconnaissance.
- **coders** may receive relevant excerpts later, but this is lower-value because they operate after the plan has concrete file/task scope and must read current files directly.

The scout does **not** re-run between execution waves. By wave 2, wave 1 may have changed the codebase and made the original brief stale. Coders already get file lists in their task prompts and read current files on cheaper models.

## Artifact path and handoff contract

Use this deterministic path:

```text
docs/briefs/TODO-<id>-brief.md
```

For non-todo/freeform inputs, use a dated slug under `docs/briefs/`, but todo-backed briefs should be keyed by todo ID so `define-spec` can auto-discover them and preserve provenance into the generated spec.

`define-spec` already looks for:

```text
docs/briefs/TODO-<id>-brief.md
```

and preserves this provenance line in generated specs:

```text
Scout brief: docs/briefs/TODO-<id>-brief.md
```

`generate-plan <spec-path>` may continue to extract a valid `Scout brief:` preamble line from a reviewed spec and pass the path through to the planner and `refine-plan`. It should not gain a brief-only or direct-todo planning path; the spec is the handoff artifact after mandatory user Q&A.

## What the scout produces

A structured scout brief, written under `docs/briefs/`.

Recommended format:

```markdown
# Scout Brief: <task title>

Source: TODO-<id>                    <- when todo-backed
Generated at: <ISO timestamp>
Git SHA: <sha>
Model: <provider/model>

## Summary

Short task-scoped overview of the relevant codebase area.

## Broad Orientation

Brief repo/package/module orientation: relevant top-level areas, boundaries, likely entry points, test layout, and adjacent areas that may affect the task.

## Relevant Files

Files directly related to the task, each with a one-line role and why it matters.

## Key Interfaces and Types

Contracts, exported types, commands, events, schemas, or APIs the task must understand or modify.

## Dependency / Call Graph

How relevant files relate: imports, calls, implements, registrations, event flow, command flow.

## Patterns and Conventions

Concrete patterns used in the affected area: naming, file organization, error handling, testing style, configuration, dispatch/registration conventions.

## Existing Tests and Test Patterns

Current coverage for the affected code and examples of how similar behavior is tested.

## Risk Areas

Widely depended-on files, brittle seams, stale/generated artifacts, migration hazards, concurrency/state risks, or places where similar changes have broken before.

## Disconfirmation / Possible Misses

Anti-bias checks performed: named files that were incomplete or misleading, adjacent implementations and alternate call paths checked, contradictions between task framing and code reality, and plausible areas the scout may still have missed.

## Precedents and Lessons

Similar prior changes and follow-up fixes, with commit hashes when git history is available.

## Open Questions / Ambiguities

Questions the scout cannot answer from code alone. These are not asked by scout directly; they are inputs to `define-spec`.

## Confidence Notes

Where the scout is confident vs uncertain, especially if using a cheaper model.
```

This keeps the useful parts of `rpiv-pi` research while making the brief a stable machine-readable artifact for downstream skills.

## Q&A policy and process streamlining

`rpiv-pi`'s `research` skill has a developer checkpoint after analysis and before writing the research artifact. That is appropriate when research itself is the user-facing deliverable, but it is a poor default fit for this pipeline when `define-spec` follows.

Default scout behavior should be **non-interactive**:

- Scout should not ask the developer questions in its own loop.
- Scout should write unanswered questions into `## Open Questions / Ambiguities`.
- `define-spec` should remain the single user-facing Q&A loop for intent, scope, and architecture.
- `define-spec` should use the scout's open questions to ask better, codebase-grounded questions without duplicating a prior scout checkpoint.

Rationale:

1. Avoid two consecutive Q&A loops (`scout` then `define-spec`) asking overlapping questions.
2. Keep scout as cheap reconnaissance, not a deliberation stage.
3. Let `define-spec` ask questions at the right time: after codebase reality is known, but while user intent and requirements are being clarified.
4. Preserve one clear owner for human decisions.

Exception: if the user runs scout as a standalone research deliverable, an optional interactive mode could be added later. That should not be the default pipeline path.

## No direct-to-plan behavior

This workflow should not support `scout → generate-plan`, `brief → generate-plan`, or direct todo-to-plan as normal paths. `define-spec` is mandatory because it is the only stage that owns user Q&A for intent, scope, and architecture decisions.

Scout open questions should flow into `define-spec`, not directly into planning. `define-spec` should either resolve those questions with the user or explicitly document unresolved, load-bearing ambiguities in the spec before planning begins.

If a future lightweight execution path is added, it should still start after mandatory user Q&A:

```text
define-spec Q&A → lightweight plan → lightweight serial execution
```

That path may use simpler serial subagents instead of the full parallel planning/execution pipeline, but it should not bypass the spec/Q&A gate.

## Design questions

### 1. When should the scout run?

Options:

- **Always before planning.** Simple, but adds latency and cost for well-scoped tasks where the relevant files are already known.
- **Only when the todo/freeform request lacks codebase context.** Skip if the input already names specific files/modules and the repo is small enough. Requires a heuristic.
- **User-triggered.** Explicit opt-in. Least friction for simple tasks but requires the user to judge when it is worthwhile.

Recommended initial policy: **user-triggered plus auto-suggested**. Provide a scout skill/command, and have `define-spec` suggest or offer it during preflight when the input is broad, touches unknown codebase areas, or lacks file/module anchors.

### 2. Is a cheap model good enough?

The scout needs to:

- Follow imports and trace dependency chains — cheap models can often do this reliably.
- Identify patterns and conventions — less certain.
- Characterize design intent and risk areas — judgment-heavy and risky on very cheap models.

Because both `define-spec` and planner may treat the brief as trusted input, default to **standard/Sonnet-tier** unless the brief is constrained to factual extraction only. If using a cheap model, require explicit confidence notes and bias the prompt toward literal evidence: paths, imports, call sites, tests, and quoted patterns rather than design interpretation.

### 3. How does the planner consume the brief?

Options:

- **Injected into the planner prompt.** Simple, but increases prompt size.
- **Referenced as a file.** Keeps prompt lean; planner reads it as needed.
- **Replaces broad exploration.** Prompt says the scout already mapped the relevant area; planner should use the brief for orientation, then verify task-critical files directly.

Recommended: **referenced as a file from the spec and load-bearing**. Planner must read the brief when the spec includes a `Scout brief:` provenance line, use it for orientation, and still read task-critical files directly before producing tasks.

### 4. What are skip conditions?

Skip or avoid suggesting scout when:

- The repo is tiny and a planner can read the relevant code directly.
- The request already names exact files, interfaces, tests, and constraints.
- The task is docs-only or configuration-only with obvious scope.
- A brief already exists for the same todo at the current git SHA.
- The input is too vague to scout meaningfully; run `define-spec` first to clarify anchors.

### 5. Brief format and staleness

The brief should not include full file contents by default. It should include structured summaries, paths, line references where useful, and concrete snippets only for non-obvious conventions. Downstream agents should still read current files for task-critical implementation.

The brief must include:

- generated timestamp
- git SHA
- source todo path or freeform source metadata
- model used
- confidence/uncertainty notes

Downstream consumers should treat a brief generated at a different git SHA as potentially stale and warn before relying on it.

### 6. Is the brief required input or advisory?

If scout exists, make the brief **required when present**:

- `planner` must read and incorporate it.
- `plan-reviewer` must check that important brief findings are reflected in the plan, especially relevant files, risk areas, tests, and conventions.
- If the plan intentionally deviates from the brief, it should record why in `## Risk Assessment`.

If we are not willing to enforce that, then the brief is aspirational and should be dropped from provenance. Recommended: enforce it.

### 7. Should the scout receive the todo, or index generically?

Recommended: **give the scout the full todo/freeform task, but require broad orientation and disconfirmation**.

A generic whole-codebase index can be useful later as a separate durable repo map/cache, but it is not the best default brief for this pipeline. Downstream agents need task-relevant constraints, interfaces, tests, and risks. Without the task, a scout cannot reliably distinguish generally important architecture from the parts that matter for this change. Generic indexing is also slower, more expensive, more likely to go stale, and may still miss obscure task-specific seams.

A task-aware scout does risk anchoring on the todo's wording, so the scout must explicitly counter that bias:

- Do a broad orientation pass before the task-focused deep dive.
- Do not assume files or modules named in the todo are complete or correct.
- Search for adjacent implementations, alternate call paths, registrations, tests, and generated/configured entry points.
- Record contradictions between the task framing and code reality.
- Include possible misses and confidence limits in the brief.

This makes the scout focused enough to save tokens while less likely to blindly reinforce the user's initial framing.

## Integration work

Required changes:

1. Add a scout skill/agent adapted from `rpiv-pi` research mechanics, using the upstream research skill as a reference: https://github.com/juicesharp/rpiv-mono/blob/main/packages/rpiv-pi/skills/research/SKILL.md
2. Encode broad-orientation and disconfirmation requirements in the scout prompt.
3. Write briefs to `docs/briefs/` using deterministic todo-backed paths.
4. Update `define-spec` documentation/prompt to treat `## Open Questions / Ambiguities` from the brief as input to its Q&A loop.
5. Ensure `define-spec` remains the mandatory user Q&A/spec gate and preserves valid scout provenance in generated specs.
6. Update `generate-plan` to consume scout briefs only through a spec's `Scout brief:` preamble; do not add direct todo/brief-to-plan behavior.
7. Update planner prompt to require reading the scout brief when present and to use it as orientation while still verifying task-critical files.
8. Update plan-reviewer prompt to include brief-coverage checks.
9. Add stale-brief handling based on git SHA.
10. Add skip/suggest heuristics.

## Token cost estimate

Rough comparison for a medium-complexity task in a large codebase:

| Approach | Exploratory reads | Model | Estimated cost |
|----------|------------------|-------|----------------|
| No scout | Planner does all discovery | capable | 150–200K input tokens at capable-tier pricing |
| With scout | Scout does discovery, planner consumes brief | standard + capable | ~100K standard + ~80K capable |
| Cheap factual scout | Scout reports mostly literal references/confidence notes | cheap + capable | ~100K cheap + ~80K capable, but higher risk of missed conventions |

The savings come from shifting broad discovery away from the planner. The planner still reads the brief and the task-critical files directly, but skips much of the exploratory import/grep phase.

## Relationship to other todos

- **TODO-ccbbedd6 (define-spec):** The scout brief is a natural input to mandatory define-spec. If both exist, the preferred pipeline is `scout → define-spec → generate-plan`.
- **TODO-0aac17a1 (fastlane):** Fastlane/lightweight execution may eventually provide a simpler post-Q&A route, likely with lightweight planning and serial execution rather than full parallel subagents. It should still not bypass mandatory define-spec Q&A.
- **TODO-e4fbf1e9 (feedback loop):** Scout output could eventually incorporate learned codebase-specific patterns, e.g. "when touching API routes, always inspect middleware chain and auth registration."

## Completion criteria

- Decide the scout's default Q&A policy: non-interactive in the pipeline, with open questions written into the brief for `define-spec`.
- Specify that the scout receives the full todo/freeform task as input, rather than running as a generic whole-codebase indexer by default.
- Specify the scout brief schema and deterministic `docs/briefs/` path conventions.
- Specify how the scout adapts `rpiv-pi` research mechanics: scope tracing, grouped analysis, code references, integration points, precedents, open questions.
- Specify the mandatory broad orientation pass and disconfirmation step that reduce overfitting to the todo framing.
- Define integration with `define-spec`, including how brief open questions feed the spec Q&A loop.
- Define integration with `generate-plan` through spec provenance only; remove/avoid direct todo or brief-to-plan behavior.
- Define planner and plan-reviewer contracts for load-bearing brief consumption.
- Define skip/suggest heuristics and stale-brief handling.
- Decide model tier policy and confidence-note requirements.
- Provide token cost comparison for representative codebase sizes.
